const f = arr => {};

describe("sorting", () => {
  it("(╯°□°）╯︵ ┻━┻", () => {
    expect(f([1, 2, 4, 3])).toEqual(2);
    expect(f([4, 2, 1, 3])).toEqual(0);
    expect(f([1, 2, 3, 4])).toEqual(4);
  });
});
